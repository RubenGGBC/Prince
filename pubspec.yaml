name: untitled2
description: "La fakin mejor app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # 🗄️ Base de Datos
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path: ^1.8.3
  crypto: ^3.0.3

  # 🎨 UI y Fuentes
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  # 🔥 Firebase y APIs
  firebase_core: ^2.32.0
  http: ^1.1.2
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  markdown_widget: ^2.3.2+6

  # 🎨 Efectos visuales
  shimmer: ^3.0.0
  lottie: ^2.7.0

  # 🆕 ========== ML KIT - VERSIONES COMPATIBLES ==========

  # 📹 Cámara
  camera: ^0.11.1

  # 🧠 ML Kit - Versiones con namespace correcto
  google_mlkit_pose_detection: ^0.6.0

  # 🔐 Permisos
  permission_handler: ^12.0.1

  # 🛠️ Utilidades para ML Kit
  image: ^4.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true